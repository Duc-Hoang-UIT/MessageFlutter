import 'package:flutter/material.dart';

class MenuLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text("Menu Layout"),
    ));
  }
}
